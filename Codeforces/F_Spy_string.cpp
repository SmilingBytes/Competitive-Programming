/**
 *    author:  ismail
 *    created: 28.06.2020  15:37:34
 **/
#include <bits/stdc++.h>

using namespace std;

typedef long long ll;
#define w(a) std::cerr << #a << " : " << (a) << "\n";

void solve() {
  int n, m;
  cin >> n >> m;
  string a[n];
  for (int i = 0; i < n; i++) cin >> a[i];

  char ans[m];
  for (int j = 0; j < m; j++) {
    for (int i = 1; i < n; i++) {
    }
  }
}

int main() {
  ios_base::sync_with_stdio(false);
  cin.tie(0);

  int test;
  cin >> test;
  while (test--) solve();

  return 0;
}