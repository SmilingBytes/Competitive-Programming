#include <bits/stdc++.h>
using namespace std;

typedef long long ll;
#define w(a) cerr << #a << " : " << (a) << "\n";

void solve() {
  string s;
  getline(cin, s);
  cin >> s;
  cout << s << '\n';
}

int main() {
  ios_base::sync_with_stdio(false);
  cin.tie(0);

  // mp.max_load_factor(0.25);mp.reserve(512);
  solve();
  return 0;
}